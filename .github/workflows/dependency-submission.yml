name: Dependency Submission

on:
  push:
    branches:
      - dev

jobs:
  dependency-submission:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    if: github.actor != 'dependabot[bot]' && github.actor != 'app/dependabot'

    steps:
      - uses: actions/checkout@v6
        with:
          submodules: recursive

      - name: Setup Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 25

      - name: Normalize Android SDK platform aliases
        run: bash scripts/ensure_android_sdk_platform_alias.sh

      - name: Validate security override files
        env:
          GH_TOKEN: ${{ secrets.GH_PAT || secrets.GITHUB_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GH_PAT || secrets.GITHUB_TOKEN }}
        run: |
          chmod +x scripts/sync_security_overrides.sh
          scripts/sync_security_overrides.sh --check

      - name: Submit Dependency Graph
        uses: gradle/actions/dependency-submission@v6
        with:
          additional-arguments: -I gradle/security-overrides.init.gradle
