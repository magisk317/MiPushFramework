package com.xiaomi.mipush.sdk

/*
 * No stock 7.4.67-C same-path source was found in the split source tree.
 */
enum class AssemblePush(private val mValue: Int) {
    ASSEMBLE_PUSH_HUAWEI(1),
    ASSEMBLE_PUSH_FCM(2),
    ASSEMBLE_PUSH_COS(3),
    ASSEMBLE_PUSH_FTOS(4)
}
